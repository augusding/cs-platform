=== CS Platform 开发进度 ===
最后更新：2026-04-11

[已完成]
- 预备期：项目骨架、docker-compose、目录结构、CLAUDE.md、开发规范文档
- Week 1 Day 1-2：4张认证表迁移（tenants/users/invitations/refresh_tokens）
- Week 1 Day 3-4：Auth API（register/login/refresh/logout/invite/accept）
  store层：tenant_store / user_store / invitation_store / refresh_token_store
  auth层：jwt_utils / password
  路由：api/routes/auth.py 已注册
- Week 1 Day 5：test_auth.py 全链路验证（13 cases 全部通过）
  覆盖：注册/重复注册/登录/密码错误/刷新/旧token轮换/登出/邀请/接受邀请/无效token/租户隔离
- Week 2 Day 1-2：bots/knowledge_sources/faq_items 三张表 + Bot CRUD API
  迁移：a005/a006/a007 已应用
  store：bot_store.py 完整 CRUD（create/list/get/update/delete/rotate_api_key/count）
  路由：api/routes/bots.py 六端点（含 free plan 配额 402）
  冒烟验证：CREATE→201 / LIST / GET / UPDATE / ROTATE / 配额限制 / DELETE 全部通过
- Week 2 Day 3-5：知识库摄取 Pipeline
  knowledge/parser.py（PDF/Excel/Word）/ chunker.py（中英文分块）
  knowledge/embedder.py（Qwen text-embedding-v3，批量 25/批）
  knowledge/vector_store.py（Milvus，每 bot 独立 collection）
  knowledge/ingestion.py（RQ Worker 入口，asyncpg 更新状态）
  api/routes/knowledge.py：上传/列表/详情/删除/FAQ CRUD
  冒烟验证：上传 docx → 201 pending + job_id；LIST/GET/FAQ/DELETE 均通过
  注意：pymilvus 依赖（marshmallow 兼容性）需修复才能实际完成摄取
- Week 3 Day 1-3：Agentic RAG Pipeline 6 节点 + 编排器
  core/rag/router.py（意图分类 + simple-pattern skip）
  core/rag/query_transform.py（HyDE / Step-Back / Expansion）
  core/rag/retriever.py（hybrid：FAQ + vector + BM25 加权）
  core/rag/grader.py（top-3 平均分，阈值触发 re-retrieve，≤2 次）
  core/rag/generator.py（流式 + Qwen→DeepSeek fallback）
  core/rag/hallucination_checker.py（JSON grounded 判断，fallback 短语短路）
  core/engine.py（run_pipeline 编排 + re-retrieve 循环 + out_of_scope/transfer/clarify 分支）
  store/__init__.py 重导出所有 store 子模块
  端到端验证：'你好' → skip_retrieval 路径 → Qwen 流式 5 tokens → 1374ms

- Week 3 Day 4-5：WebSocket Chat + Widget JS + Standalone 对话页
  迁移：a008 (sessions/messages/leads) 已应用
  store/session_store.py：create/get_or_create/save_message/get_history/close
  api/routes/chat.py：WebSocket /api/chat/{bot_id}，协议帧 connected/token/done/error/transfer/ping/pong
    安全：path bot_id 与 API Key 解析的 bot_id 必须匹配（防跨 bot 使用）
  api/routes/widget.py：GET /widget.js（公开）+ GET /chat/{bot_id} standalone 页
    standalone 页 HTML-escape 所有嵌入字段，防 XSS
  widget/widget.ts + widget.js：Shadow DOM 隔离，apiKey 从 CS_CONFIG 读取
  middleware：/chat/{bot_id} 前缀纳入 PUBLIC_PATHS
  端到端：login→create bot→rotate key→WS connect→发'你好'→收 5 tokens→done
         1341ms grounded=True，DB 保存 user+assistant 2 条消息

- Week 4 Day 1-2：语义缓存激活
  cache/semantic.py：key=semantic:{bot_id}:{md5(query)}，TTL 86400s
  cache/session.py：会话历史缓存，TTL 1800s，最多保留 10 轮
  cache/quota.py：月度消息计数 INCR + expireat 到月底
  core/engine.set_redis()：app startup 注入 redis，_check/_write_semantic_cache 激活
  chat.py：优先从 session 缓存读历史，回退 DB；写回 session 缓存 + 配额递增
  关键修复：skip_retrieval 分支原先直接 return 跳过 cache write，已补上
  端到端：同一'你好'query 第 1 次 1589ms MISS → 第 2 次 1ms HIT (60x 加速)
- Week 4 Day 3-5：Admin Console MVP
  api/routes/admin.py：GET /api/admin/sessions (分页) + /api/admin/stats
  frontend/：Vite + React 18 + TS + Tailwind v4 + react-router-dom 项目初始化
  5 个页面：Login / Dashboard / Bots / Knowledge / Sessions
  frontend/src/api/client.ts：axios + JWT 自动附加 + 401 重定向登录
  构建验证：tsc --noEmit 通过；vite build 输出 214KB JS + 15KB CSS

- Week 5 Day 1-5：Lead Capture + 通知推送 + 人工接管
  store/lead_store.py：create/list/get/update_status，lead_info JSONB
  core/rag/lead_collector.py：DEFAULT_FIELDS（product/qty/price/contact）
    extract_info Qwen 单字段抽取，prompt_for / get_next_missing_field / intent_score
  core/engine.py：lead_capture 分支
    关键修复：规范用 state.lead_info 在 WS 消息间"持久化"→ 失败
    改用 Redis key=lead_state:{session_id} TTL 1h 保存部分状态
    首轮前 _load_lead_state → 存在则强制 intent=lead_capture（跳过 cache+router）
  job_queue/tasks/notifications.py：SMTP 邮件（未配置 SMTP_HOST 时静默跳过）
  api/routes/leads.py：GET/list + GET/{id} + PUT/{id} 状态更新
  api/routes/chat.py：_complete 时 create_lead + RQ enqueue 邮件；
    should_transfer 时 RQ enqueue 人工接管通知 + sessions.status=transferred
  config.py：SMTP_HOST/PORT/USERNAME/PASSWORD/FROM_NAME
  frontend/src/pages/Leads.tsx：5 页面 Admin 增加线索列表 + 状态流转
  端到端验证（多轮 LLM 驱动）：
    T1 '我想采购一批蓝牙耳机' → extract product_requirement → ask quantity
    T2 '大概需要5000个' → extract quantity → ask target_price
    T3 '目标价格20元每个以内' → extract target_price → ask contact
    T4 '我的邮箱 alice@example.com' → extract contact → 全部完成 → 感谢消息
    DB lead 入库 intent_score=1.0 status=new
    PUT /api/leads/{id} status → contacted 成功
  前端构建：tsc --noEmit + vite build → 217KB JS + 17KB CSS

- Week 6 Day 1-5：套餐/配额 + 模拟支付 + 私域引流
  迁移 a009：tenants.plan_expires_at / billing_email + orders 表
  store/plan_store.py：PLAN_CONFIG (free/entity/trade/pro)，
    get_tenant_plan / upgrade_plan / create_order / confirm_order
  config.py：WECHAT_PAY_* 四项配置占位
  api/routes/billing.py：plans / status / create-order / webhook / simulate-pay
    安全修正：规范 simulate-pay 未校验订单归属，已加 tenant_id 校验防跨租户
    simulate-pay 仅在 DEBUG=true 时可用（生产返回 404）
  api/middleware.py：
    - /api/billing/plans 和 /webhook 加入 PUBLIC_PATHS（status/create-order/simulate-pay 仍走 JWT）
    - bot-key 分支追加月度配额预检：check_limit 过则 402，不在此处 increment（避免与 chat.py 双计数）
  api/routes/chat.py：私域引流帧推送（bot.private_domain_config 配置时，每 3 条 knowledge_qa 消息推一次 QR）
  widget/widget.js：private_domain 帧处理（消息 + QR 图卡片）
  frontend/src/pages/Billing.tsx：套餐卡片 + 当前标识 + 一键模拟升级
  端到端验证：
    BEFORE: free / 1 bot / 200
    CREATE ORDER 201 CS20260411033950913FF771 amount=299
    SIMULATE 200 → AFTER: trade / 3 / 5000 + expires 2026-05-12
    INVALID PLAN → 400；DOUBLE PAY → 400
    Quota 0 → WS handshake 402 ✓
  前端构建：220KB JS + 17.5KB CSS

- Week 7：CI/CD + Golden Set + 安全加固 + 上线准备
  tests/golden_set/cases.json：10 条基础场景（greeting/oos/transfer/lead/security，中英各半）
  tests/test_golden_set.py：自动登录→创建临时 bot→rotate key→跑用例→清理
    评分：grounded(0.3) + expected_contains OR 匹配(0.4) + transfer(0.3) - penalty
    首轮自动写 baseline，后续对比；delta < -0.05 标记 REGRESSED 并退出非零
  tests/test_tenant_isolation.py：8 个用例（注册 A+B、A 建 bot、B list/GET/PUT/DELETE/admin 全部 403 或隐藏）
  tests/locustfile.py：50 并发压测模板（/health, /bots, /billing/plans, /admin/stats）
  tests/run_all.py：一键回归 + 基础安全检查（.env git history, sanitize_input）
  .github/workflows/ci.yml：postgres+redis services → migrations → 后台起 server →
    /health wait → test_auth + test_tenant_isolation → frontend tsc + build
  api/middleware.py：rate_limit_middleware（IP/分钟窗口 deque）+ cors_middleware（OPTIONS 204）
  api/app.py：中间件链 = [cors, rate_limit, jwt]
  main.py：JSON 结构化日志（_JsonFormatter 捕获所有 extra 字段，保留 exc_info）
  config.py：RATE_LIMIT_PER_MIN / WIDGET_ALLOWED_ORIGINS
  迁移 a010：orders.tenant_id FK → ON DELETE CASCADE（修复 Week 6 遗留的 test cleanup 阻塞）
  tests/test_auth.py cleanup_test_data：按依赖顺序清 child 表（child → user → tenant）
  端到端验证：
    test_auth 13/13 ✓
    test_tenant_isolation 8/8 ✓
    CORS OPTIONS → 204 + headers ✓
    rate limit 310 burst → 280×200 + 30×429 ✓
    frontend build 220KB ✓

- Phase 1：基础修复 + 生产环境就绪
  Milvus 兼容性：pin marshmallow<3.13，pymilvus 2.4.0 导入正常
  RQ Worker：docker-compose 新增 worker service（profile=worker 可选启动）
    Dockerfile.worker 独立镜像
    scripts/start_worker.ps1 本地 Windows 启动脚本
  /health/detail：新增 rq_queue 和 milvus 检查
    critical = {postgres, redis}；rq_queue / milvus 失败仅 warning 不降级
  Bot API Key reveal-key：
    store/bot_store.get_bot_with_key() admin-only
    POST /api/bots/{id}/reveal-key → 返回完整 key + 可复制的 <script> 嵌入代码
    embed_code 基于 request.scheme + host 自动生成 widget 引用
  前端交互完善：
    api/client.ts：401→重定向, 402→CustomEvent('quota-exceeded'),
                   429→CustomEvent('rate-limited')
    Bots.tsx：嵌入代码 Modal + 复制按钮 + 创建表单 inline error
    Knowledge.tsx：pending/processing 状态 5s 自动轮询，上传后立即刷新
  端到端验证：
    /health/detail → postgres/redis/rq_queue/milvus 全部 ok
    reveal-key → 完整 key + embed_code 含 CS_CONFIG/botId/apiKey
    revealed key WS 连接成功
    Milvus create collection → drop 完整生命周期 OK

[进行中]
  （Phase 1 完成，v1.0.0 MVP + 知识库摄取 Pipeline 全线可用）

[待开始]
  （本里程碑结束后）

[已知问题/注意事项]
- queue/ 目录已改名为 job_queue/（避免与Python内置queue模块冲突）
- alembic.ini 中文注释已改为英文（Windows GBK编码问题）
- 开发阶段邀请token直接在API响应中返回，生产环境需改为发邮件
- pymilvus 2.4.0 依赖 environs → marshmallow<3.13；已在 requirements.txt pin，
  本地环境 pip install "marshmallow<3.13" 已修复（Phase 1）

[安全]
- P0 已实现：
  * docs/SECURITY.md 沙箱架构 + 上线检查清单
  * core/engine.sanitize_input()：prompt 注入过滤 + HTML/控制字符剥离 + 2000 字符截断
  * api/routes/knowledge.py 上传 20MB 限制（超限 413 并删除临时文件）
- P1 待办（Week 7 统一实现）：
  * 敏感词过滤 / PII 检测（手机号/身份证）
  * PostgreSQL Row-Level Security
  * API 限流（token bucket，超限 429）
  * WIDGET_ALLOWED_ORIGINS 收紧（非 *）
  * 强制 HTTPS

[仓库]
GitHub: https://github.com/augusding/cs-platform
分支: main
